class QuizScores {
  kanji: number;
  hiragana: number;
  katakana: number;

  constructor(kanji: number, hiragana: number, katakana: number) {
    this.kanji = kanji;
    this.hiragana = hiragana;
    this.katakana = katakana;
  }
}

export default QuizScores;
